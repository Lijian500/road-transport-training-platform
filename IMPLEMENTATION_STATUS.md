# 项目实施进度

> 最后更新：2026-07-29
> 当前阶段：阶段一——仓库和基础环境初始化
> 总体状态：本地工程骨架已完成，动态运行验证部分受环境限制

## 1. 文档用途

本文档持续记录项目实际实施进度、已完成内容、当前阻塞项和下一阶段计划。每完成一个可验证阶段，都应同步更新本文档，避免README中的设计目标与仓库实际状态混淆。

状态约定：

- `✅`：已经实现并完成相应验证；
- `🚧`：正在实施或仅完成部分内容；
- `⬜`：尚未开始；
- `⛔`：受外部条件阻塞。

## 2. 当前实施阶段

### 阶段一：仓库和基础环境

| 工作项 | 状态 | 验证依据 |
|---|---|---|
| 创建全新项目目录，与原商业项目隔离 | ✅ | 独立目录和独立Git仓库 |
| 确定Maven坐标和Java包名`me.lj.train` | ✅ | 父POM及Java源码目录 |
| 建立顶层README设计文稿 | ✅ | 根目录`README.md` |
| 建立后端Maven多模块骨架 | ✅ | 18个Maven模块通过JDK 17离线`validate` |
| 建立Vue 3 TypeScript前端骨架 | 🚧 | 静态结构通过，依赖安装和构建因网络超时待验证 |
| 建立Docker Compose基础设施 | 🚧 | YAML解析通过，本机未安装Docker，未实际启动 |
| 建立MySQL逻辑数据库初始化脚本 | ✅ | 三库三账号且账号权限互相隔离 |
| 建立GitHub Actions基础CI | ✅ | 两份工作流通过YAML语法解析 |
| 初始化本地Git并创建初始提交 | ✅ | 本阶段结束前创建`main`分支初始提交 |
| 创建GitHub远程仓库并推送 | ⛔ | 当前环境缺少GitHub CLI，需要后续安装并登录 |

## 3. 尚未进入的阶段

| 阶段 | 状态 | 主要内容 |
|---|---|---|
| 阶段二 | ⬜ | 登录、企业、组织、人员、角色和权限 |
| 阶段三 | ⬜ | 课程、课件、培训计划和学员任务 |
| 阶段四 | ⬜ | 学习会话、状态机和HTTP验证入口 |
| 阶段五 | ⬜ | WebSocket实时学习、心跳和异常恢复 |
| 阶段六 | ⬜ | 人脸抽验业务流程 |
| 阶段七 | ⬜ | 题库、考试、培训完成和统计 |
| 阶段八 | ⬜ | 完整测试、部署材料和论文图表 |

## 4. 当前明确不包含的内容

本阶段只创建工程、配置、目录、启动入口和验证脚本，不实现以下业务：

- 用户登录和权限业务；
- 企业、组织、人员和车辆CRUD；
- 课程、培训计划和考试业务；
- WebSocket学习消息处理；
- 有效学时计算；
- 人脸核验接入；
- 业务数据库表和演示数据。

## 5. 验证记录

| 验证项 | 结果 |
|---|---|
| 全仓库无BOM UTF-8检查 | 114个文件检查通过 |
| Maven工程模型 | 18个模块通过JDK 17离线`mvn validate` |
| Maven完整构建 | 首次依赖下载超时，离线环境缺少部分Spring依赖JAR，待联网重试 |
| XML与JSON | 18个XML、5个JSON解析通过 |
| YAML | 9个YAML文件解析通过 |
| Java包名 | 14个Java文件均以`me.lj.train`开头 |
| PowerShell脚本 | 3个脚本语法解析通过 |
| Compose变量 | 27个变量均能在根目录`.env.example`找到 |
| 数据库配置 | 后端三服务与Compose使用相同的三组数据库变量 |
| 前端依赖和构建 | 离线缓存缺少依赖，尚未生成`pnpm-lock.yaml` |
| Docker运行 | 当前机器未安装Docker，尚未执行真实容器启动 |

## 6. 当前阻塞项

1. 当前环境没有GitHub CLI，无法进行GitHub身份校验、创建远程仓库和推送。
2. 前端依赖安装访问包仓库超时，`pnpm-lock.yaml`和动态构建验证待补充。
3. Maven首次下载部分Spring依赖超时，完整`clean verify`待网络恢复后执行。
4. 当前机器没有Docker，Compose只能进行静态配置检查。
5. 系统默认`JAVA_HOME`未指向JDK 17，执行后端命令前需要切换到JDK 17。

## 7. 下一步

1. 配置JDK 17为项目构建环境。
2. 网络恢复后生成并提交`frontend/pnpm-lock.yaml`，运行前端检查和构建。
3. 联网执行后端`mvn clean verify`。
4. 安装Docker Desktop后启动并检查全部基础设施。
5. 安装并登录GitHub CLI后创建私有远程仓库并推送。
6. 经确认后进入阶段二，优先实现登录和最小RBAC闭环。

## 8. 更新规则

- 每次功能提交同步更新“当前实施阶段”和对应工作项；
- 只有代码、配置和验证结果都存在时才能标记为`✅`；
- 未经测试的目标值不得写成已经达到的结果；
- 阶段发生变化时更新顶部的当前阶段和总体状态；
- 外部阻塞解除后，应删除或改写对应的`⛔`说明。
