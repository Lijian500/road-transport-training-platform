# 项目必须遵守的规则

本文件适用于整个仓库。修改前同时阅读相关目录的说明，按本次任务的实际范围执行。

## 开发约束

- 默认中文交流，结论简洁、直接、可执行；文本文件统一使用无 BOM 的 UTF-8。
- 优先最小必要修改，避免无关重构、批量格式化、包结构整理和命名清洗。
- 本项目后端使用 JDK 17，遵循现有 Maven 配置和 `me.lj.train` 包名；按现有风格使用 Lombok。
- 对外 RPC 返回明确泛型的 `Result<T>`，没有返回数据时使用 `Result<?>`；新方法优先采用 `Result.ok()`、`Result.failed()` 等现有静态方法。
- 业务服务不得直接访问其他服务的数据表；数据库结构变化追加 Flyway 迁移，不修改已在共享环境执行过的迁移。
- 前端遵循现有 Vue、TypeScript 和 pnpm 约束；每个方法必须有注释。新增代码补充必要中文注释，可读性优先，保持简洁。
- 相同逻辑涉及多个工程时，方法签名尽量一致，便于维护。
- 按改动风险做针对性验证；简单文档或低影响改动不编译整个项目。报告实际执行的检查，不把已有测试文件当作本次测试通过。
- 不提交密钥、真实人脸照片、客户数据、原商业项目代码或内部资源；保留用户已有的未提交改动。

## 需求、文档与论文同步（强制）

流程、模板和待处理清单统一维护在[变更总索引](docs/changes/README.md)，不得另建互相独立的论文变更账本。

1. 完成影响业务规则、功能范围、权限、流程、数据模型、关键机制、实验结论或论文截图的改动时，必须在同一任务中维护对应批次记录和总索引，不等到写论文时再回忆。无论文影响的纯内部调整简记原因，不扩写成业务需求。
2. 记录变更来源、旧规则与新规则、代码定位、实现状态、验证证据、受影响章节及论文同步状态。需求原因不可考时写明“根据代码归纳”；未提交代码记录审查时间、基准提交和文件范围，不推断实际修改日期。
3. 小改动默认增量维护：阅读相关条目、指定代码及必要调用链，只修正文档的受影响部分；修改代码默认同步记录，不默认重写 Word 论文。
4. 更新论文前，必须将论文上次同步的代码范围与当前 Git 差异对照，包含已提交、暂存、未暂存及未跟踪源码。文件清单只能辅助定位，还应核对相关方法的后续变化；发现漏记先补记录，不能仅凭索引认定完整。
5. 核心业务主线或架构广泛变化、论文代码基线不明、遗漏影响无法界定，以及论文定稿前，必须做项目整体一致性核查；核查涵盖需求、实现、数据库、接口、测试、图表及结论，随后仍按实际影响修改章节。局部影响明确时不扩大成全量重写。
6. 分清预期需求、实际代码和验证证据。三者不一致时记录差异；“已实现”“已有测试代码”“已运行通过”分别表述。历史测试结论限定其版本、环境和场景，不自动覆盖新代码，不将模拟或夹具结果写成真实闭环。
7. 论文更新完成后，回填论文文件及版本、已处理编号、实际修改章节、覆盖的代码范围和剩余事项；部分处理不能把整批标记为已同步。只更新技术底稿不等于更新了 Word 论文。
8. 当前说明出现确证过时内容时定向修正并链接变更记录；历史验收报告保留原始日期和结果。无法核实的新实验结果标明待验证，不补造数据。

阶段性功能完成后仍需同步[实施进度](IMPLEMENTATION_STATUS.md)。提交与验证约定见[贡献说明](CONTRIBUTING.md)。
