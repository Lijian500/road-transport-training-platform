# 架构设计

本目录维护可独立评审的系统架构资料，包括：

- 系统上下文、容器和模块关系图；
- 网关、BFF、Dubbo服务与实时通信服务的职责边界；
- 同步调用和异步事件链路；
- 学习状态机、有效学时计算及异常恢复设计；
- 数据、缓存、消息和文件存储边界；
- 关键架构决策记录（ADR）。

顶层架构以仓库根目录`README.md`为入口。进入详细设计阶段后再拆分专题文档，避免在多个文件重复维护同一规则。
